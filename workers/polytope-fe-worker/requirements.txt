polytope-mars==0.3.12
polytope-python==2.1.17
covjsonkit==0.2.22
PyYAML==6.0.3
pyfdb==5.22.0.26
pygribjump[binary]==0.12.0.26
# Python eccodes bindings — version pinned to latest available that covers the
# eccodeslib C library shipped by pygribjump[binary] (currently 2.48.x).
# Uses findlibs at runtime so no separate libeccodes.so install is needed.
eccodes==2.47.0
